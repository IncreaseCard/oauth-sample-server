In order to execute this test you have to have Docker running
Then: 
1)Execute in console: docker build . -t terceros-test
2)Execute in console: docker run --rm -e CLIENT_ID= <YOUR-CLIENT-ID> -e CLIENT_SECRET=< YOUR-CLIENT-SECRET> -e TOKEN_HOST=https://oauth.increase.app -p 8080:8080 terceros-test`
